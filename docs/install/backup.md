# Backup your existing configuration

The Dotfiles Installer will add symlinks into your `home` and `.config` folder. Doing so will overwrite existing files and folders.

In this dialog, you can create a backup of existing files.

![image](/backupscreen-dark.png)

You can open the backup folder from the main menu in the upper right corner of the Dotfiles Installer.

